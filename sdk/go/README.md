# Go SDK (Planned)

This package will provide a Go client for Guard CAS.

- DTOs generated from `../../docs/swagger.yaml` via `oapi-codegen` (types/models only)
- Thin handwritten client for ergonomics and flows
- Conformance scenarios shared with other SDKs
