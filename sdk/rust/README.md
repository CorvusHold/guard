# Rust SDK (Planned)

This package will provide a Rust client for Guard CAS.

- DTOs generated from `../../docs/swagger.yaml`
- Thin wrapper for flows using `reqwest` + `serde`
- Conformance scenarios shared with other SDKs
