# Guard CAS - Example Environment Variables

# App
APP_ENV=development
APP_ADDR=:8080
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database (docker-compose Postgres)
DATABASE_URL=postgres://guard:guard@localhost:5433/guard?sslmode=disable

# Cache (docker-compose Valkey/Redis)
REDIS_ADDR=localhost:6380
REDIS_DB=0

# Auth
JWT_SIGNING_KEY=change-me-dev-jwt-secret
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=720h
DEFAULT_AUTH_MODE=bearer # bearer | cookie - Controls default auth mode announced in OAuth2 discovery endpoint

# SMTP (emails for verification, magic link, etc.)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-user
SMTP_PASSWORD=your-smtp-pass
SMTP_FROM=no-reply@example.com

# WorkOS (SSO)
WORKOS_API_KEY=your-workos-api-key
WORKOS_CLIENT_ID=your-workos-client-id
WORKOS_CLIENT_SECRET=your-workos-client-secret
