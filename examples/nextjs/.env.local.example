# URL of the Guard API (the public base URL)
# If running via repo Makefile test stack, use http://localhost:8080
GUARD_BASE_URL=http://localhost:8080

# Default tenant for the example app
# Use NONMFA_TENANT_ID from your .env.conformance or .env.k6 if available
GUARD_TENANT_ID=

# Max seconds to wait between retries on 429 Too Many Requests
# Defaults to 2 seconds if unset
GUARD_RATE_LIMIT_MAX_WAIT_SECS=2

# Max retry attempts for 429 Too Many Requests (used by /api routes)
# Defaults to 3 if unset
GUARD_RATE_LIMIT_MAX_ATTEMPTS=3
